import { createTheme } from "@mui/material/styles";


const fontTheme = createTheme({
  typography: {
    fontFamily:
      "MyFont-Regular, MyFont-Bold,MyFont-Medium,MyFont-Semibold, Arial, sans-serif,Custom-ExtraBold",
  },
});

export default fontTheme;